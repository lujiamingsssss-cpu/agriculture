# 任务（TASKS）

> **本文件的唯一职责**：记录**当前位置**、**路线图**、**当前任务**与**任务池**。
> **不属于本文件**：设计理由（→ `DESIGN.md`）、范围仲裁（→ `BASELINE.md`）、
> 演示交付物与提交卫生（→ `DEMO-HANDOVER.md`）。
>
> **本文件是当前任务的唯一真相源。任何未登记在此的工作，视为未获授权。**

---

## §1 当前位置（一眼看懂）

**日期 2026-09-18 · 位置：已完成 T-10，停在 T-11 之前 · 无阻塞**

| 项 | 值 |
|---|---|
| 已完成 | **T-00 ~ T-10 全部通过**（组 0 ✅ / 组 A ✅ / 组 B ✅ / 组 C 第 1 项 ✅） |
| **下一件事** | **T-11 ⭐ 时间维度的光分配 + 南北/东西对比**（demo 的唯一核心 wow） |
| 最后一条提交 | `50f47ec` T-09 批量带型对比 |
| 阻塞 | **无** |
| 待你拍板 | 工具定位是否锁定为「**行向与光分配探索器**」——见 §3 问题 P1 |
| 已砍掉 | **T-12 / T-13**（Three.js 前端页面），改走 T-17 / T-18 |

### 一句话进度

> **光已经能算准**（能量守恒、南北/东西对比成立）；
> **数字已经出来了**（大豆受光/玉米：南北 **1.05** vs 东西 **0.22**；一天内 0.05 → 0.77 → 0.04）；
> **现在缺的只是把它变成评委看得懂的东西** —— 一条日光曲线、一张并排图、一个能拖的滑块。

### 已经确立的两个关键事实（下面所有规划都建立在它们之上）

| # | 事实 | 实测依据 |
|---|---|---|
| 1 | **行比 / 带宽 / 株高 / LAI 全部不响应**，只有**时间、行向、地点、日期**有信号 | 逐个改输入实测，最大变化 ≤ 1% |
| 2 | 时间维度的信号强度是行比信号的约 **244 倍** | 全天逐 10 分钟扫描，73 个可算时刻，0 失败 |

> 因此 demo 的输入轴从「带型参数」改为「**时间 + 行向 + 地点 + 日期**」。
> 这不缩小演示效果，反而放大了——见 §2 的决策点。

---

## §2 路线图

### 2.1 关键路径

```
已完成：T-00 → T-01 → T-02 → T-03 → T-04 ⭐ → T-06 → T-07 → T-08 → T-10 ✅
                                                          （T-09 为旁支，已完成）

剩余：  T-11 ⭐ → T-17 → T-18 → T-14 → T-16   ← 全部剩余工作就在这一行
                  └─ T-15（依赖 T-14）
```

> **只按这条链执行。** 不要用「依赖」列判断先后（见 §5 执行顺序纪律，已因此出过事故）。

### 2.2 剩余任务（组 C / 组 D）

**组 C · 出图与呈现（对应判据 D1–D3）**

| 编号 | 任务 | 产出 | 验收 | 预估 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| **T-11** ⭐ | **时间维度的光分配 + 南北/东西并排对比** | 两作物日光曲线 + 并排热力图 | ① 滑块逐时拖动结果连续；② 南北「尖峰」与东西「平稳」一眼可辨；③ 同参数、同色标、同尺度 | 1d | T-10 | ⬜ **下一件事** |
| **T-17** | **三维渲染（Blender headless）** | 3D 场景图，颜色**来自计算值** | 玉米高/大豆矮可辨；色标标物理量+单位；无照向冠层的真实 Sun 灯 | 1d | T-11 | ⬜ 未开始 |
| ~~T-12~~ | ~~三维冠层（Three.js InstancedMesh）~~ | — | — | — | — | 🔴 **已砍**（属阶段二前端，见 §4） |
| ~~T-13~~ | ~~参数面板 + 实时重算~~ | — | — | — | — | 🔴 **已砍**（同上） |

**组 D · 交互与交付（对应判据 D2、D5）**

| 编号 | 任务 | 产出 | 验收 | 预估 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| **T-18** | **交互演示工具**（时间滑块 + 行向开关 + 地点/日期） | 可现场点击的页面 | 组件清单见 `DEMO-HANDOVER.md` §4；不支持项**必须显式标注** | 1d | T-17 | ⬜ 未开始 |
| **T-14** | 3 分钟演示脚本 + 录屏 | 视频 | 成片可播放，含南北/东西对比镜头 | 0.5d | T-18 | ⬜ 未开始 |
| **T-15** | 找 ≥ 3 位非团队成员看一遍，记录反馈 | 反馈记录 | 反馈写入本文件附录 | 0.5d | T-14 | ⬜ 未开始 |
| **T-16** | 阶段一收尾自检 | 自检表 | D1–D5 全部勾选 | 2h | T-15 | ⬜ 未开始 |

### 2.3 本阶段的两个决策点（已定，留档）

| # | 决策 | 原来 | 改成 | 依据 | 影响 |
|---|---|---|---|---|---|
| **D-a** | **输入轴** | 行比 / 带宽（拖滑块换带型） | **时间 + 行向 + 地点 + 日期** | 行比等输入实测**无响应**；时间信号强 244× | `BASELINE.md` §3.1 的演示目标须随之修订（**待办**） |
| **D-b** | **工具定位** | 带型推荐器 | **行向与光分配探索器** | 模型答不了行比/带宽/株高/LAI，答得了「这块地南北排还是东西排、大豆什么时候晒到太阳」 | 界面必须有「本原型不支持」的明示（`DEMO-HANDOVER.md` §2/§4） |

> ✅ **D-a 已同步修订 `BASELINE.md` §3.1 与判据 D2**（2026-09-18），
> 高层文档与本节一致，不存在悬空的冲突。
> ⚠️ 修订属**高层文档变更**：若后续要改回"以带型参数为输入轴"，
> 必须先在 `DESIGN.md` 附录 A 解决第 16 行（带型参数不响应），否则改回去等于退回"看不出差别"的演示。

---

## §3 问题 ↔ 方案绑定表 ⭐

> 本项目的每一次返修都对应下表中的某一行。**改代码前先在这一行找到原因**；
> 新增问题必须先在本表登记，再动手（`AGENTS.md` §2「先写可证伪的判据」）。

| # | 问题（已实测） | 现象 / 数值 | 绑定方案 | 实施位置 | 状态 | 什么情况下会**返修** |
|---|---|---|---|---|---|---|
| **P1** | 行比 / 带宽 / 株高 / LAI **不响应** | 逐个改输入，最大变化 ≤ 1% | 不暴露为控件；界面显式标注「本原型不支持」；改用时间/行向/地点/日期做输入轴 | T-11、T-18；`DEMO-HANDOVER.md` §4 第 8 项 | ✅ 已定 | 若阶段二重做冠层表达（`DESIGN.md` 附录 A 第 9/11/13/15 行），这些控件可重新开放 |
| **P2** | **连续行向角度能量守恒失效** | 15°–75° 区间吸收/入射 = **1.19 ~ 2.80**（> 1，物理不可能）；0°/90° 为 0.9844 | 只开放 **0° / 90°** 两档开关，不做连续角度 | `TASKS.md` §2.2 T-18；`PARAMETERS.md` §1 | ✅ 已定 | 若找到自洽的侧向遮挡配置（`DESIGN.md` 附录 A 第 13 行），可放开 |
| **P3** | C1 **字面成立但幅度仅 +0.99%** | 冠层总量 南北 16374 W vs 东西 16212.9 W；而玉米带 **−52.8%**、大豆带 **+121.7%** | 演示口径从「总量增益」改为「**带间再分配**」；用大豆/玉米比（1.05 vs 0.22）作主指标 | T-11 的图与文案；`DESIGN.md` §4.2 | ⚠️ 口径已改，**`CONVENTIONS.md` §7 的字面表述尚未修订**（冻结文件，须走 §8 流程） | 若评审要求以「总量」论证，需回到 `CONVENTIONS.md` §7 走变更流程 |
| **P4** | **LER ≈ 1.0**，低于文献区间 1.0–1.4 | 标准口径 **0.9995**；比值口径 1.9842 | **如实登记，禁止调参**；演示中说明这是当前几何的直接后果 | `DESIGN.md` 附录 A 第 15 行 | ⚠️ 已知偏离 | 若演示需要 LER 作为卖点 → 必须先解决 P5 |
| **P5** | **叶片「遮挡」与「不重复计数」互斥** | 单面 → 不阻断射线；双面 → 重复计数（东西向吸收/入射 = 137.5%）。三条内核路线均已实测失败 | 阶段一**不碰内核**；三维只做**形态示意 + 计算值着色**，不宣称侧向受光 | `DESIGN.md` 附录 A.2；`DEMO-HANDOVER.md` §8 | 🔴 阶段一不解决 | 若评委追问「为什么大豆在东西行向反而受光少」的机制 → 只能用 P3 的再分配口径回答，不得编造侧向受光 |
| **P6** | 太阳高度角 **≤ 0° 触发断言** | `radiation.py` 的 `sz > 0` 检查；实测 −5°/0° 报错，0.2° 起可算 | UI 侧**禁用滑块**并显示「太阳未升起」；极昼/极夜同样处理 | `DEMO-HANDOVER.md` §5 第 2 条；T-18 | ✅ 方案已定，**未实施** | 若演示地点/日期落到夜间 —— 演示前必须用目标地点日期预跑一遍 |
| **P7** | **两个 Python 解释器** | 项目 venv 3.11.9 vs Blender 内置 3.13.13 | 只能**文件交换**（JSON），不共享进程内对象 | `DEMO-HANDOVER.md` §7；T-17 | ✅ 已定 | 若试图在 Blender 里直接 import `stripcore` → 必失败 |
| **P8** | **没有 MCP 客户端**，驱动不了 GUI Blender | MCP add-on 在 TCP 9876 但未监听；代理无 MCP 调用工具 | 走 **headless**：`--background --factory-startup --python <脚本>` | `DEMO-HANDOVER.md` §9；T-17 | ✅ 已定 | 若要求「实时联动三维」→ 属阶段二，阶段一不做 |
| **P9** | **提交信息 BOM 污染**（已发生 **2 次**） | `%s` 看不见 BOM，提交历史被污染 | 提交前做**字节级**校验；用 `Set-Content -Encoding utf8NoBOM` | `DEMO-HANDOVER.md` §10.3；`TECH-STACK.md` DR-06 | ✅ 已加防护 | 若再次发生 → 检查是否用了 `Out-File -Encoding utf8` |
| **P10** | **非农业背景评委看不懂** | 用户原话：可视化差就看不出项目解决了什么问题 | 日光曲线 + 并排对比 + 可拖滑块；色标必须标物理量+单位 | T-11、T-17、T-18；`DESIGN.md` §7 | ⬜ 待实施 | 若 T-15 的试看反馈仍是「看不懂」→ 优先改 T-11/T-18，**不要**先做三维 |

---

## §4 阶段一任务清单（含已完成，供追溯）

> 颗粒度：每条 **0.5–2 天**，有**可独立验证的产出**。

### 组 0 · 开工准备

| 编号 | 任务 | 验收 | 预估 | 依赖 | 状态 |
|---|---|---|---|---|---|
| **T-00** | 仓库接入与首次提交 | 远程有首个提交，本地=远程 | 10min | — | ✅ 已完成（`67f8fe4`） |

### 组 A · 把光算出来（判据 D1）

| 编号 | 任务 | 产出 | 验收 | 预估 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| **T-01** | 跑通候选开源辐射模型 | 候选对比表 | 至少一个可 import | 0.5d | — | ✅ 已完成（选定 `pyhelios3d`） |
| **T-02** | 最小条带场景（2:4） | 场景配置 | V1–V4 全绿 | 0.5d | T-01 | ✅ 已完成 |
| **T-03** | 逐层吸收辐射 + 分离两作物带 | `radiation.py` | W1–W6 全绿；A2 判定**成立** | 1d | T-02 | ✅ 已完成 |
| **T-04** | ⭐ 判据 C1：南北 vs 东西 | 测试脚本 | 南北 > 东西（+0.90% ~ +1.01%，信噪比 35× ~ 151×；随抽样波动） | 0.5d | T-03 | ✅ 已完成 |
| **T-05** | 降级方案（A2 不成立时用） | 简化算法 | — | 1d | T-02 | ⬜ **未触发**（A2 成立） |

### 组 B · 出数字（判据 D4）

| 编号 | 任务 | 产出 | 验收 | 预估 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| **T-06** | 吸收辐射 → 干物质（RUE 法） | 换算函数 | 两作物分别出干物质 | 0.5d | T-03 | ✅ 已完成 |
| **T-07** | 单作基准（0:1 / 1:0，**同一代码路径**） | 基准值 | 与间作同一路径 | 0.5d | T-06 | ✅ 已完成 |
| **T-08** | LER 计算 | LER 数值 | 标准口径 0.9995（已知偏离，见 P4） | 2h | T-07 | ✅ 已完成 |
| **T-09** | 批量 3 种带型对比 | 对比表 | 2:3 / 2:4 / 4:4 | 0.5d | T-08 | ✅ 已完成 |

### 组 C / 组 D

见 §2.2。**T-10（剖面热力图）已完成**（`041a8f7`），T-12 / T-13 已砍。

### 已砍任务的理由（留档，避免重提）

| 编号 | 原任务 | 砍掉理由 | 替代 |
|---|---|---|---|
| T-12 | 三维冠层（Three.js InstancedMesh） | `BASELINE.md` §3.3 明确把「精致前端」列入阶段二不做项；建 Vite 工程属越界 | **T-17**（Blender headless，不动前端工程） |
| T-13 | 参数面板 + 实时重算 | 同上；且其卖点「切带型看光斑变化」已被证伪（P1：带型不响应） | **T-18**（时间滑块 + 行向开关） |

---

## §5 执行顺序纪律（2026-09-18 增补，起因：T-10 被提前执行）

**任务必须按 §2.1 的「关键路径」顺序执行，不得仅凭「依赖」列判断可否开工。**

| 列 | 含义 | 是否表示执行顺序 |
|---|---|---|
| 依赖 | **技术前置**：缺了它任务做不出来 | ❌ 只表示可行性 |
| **关键路径** | **执行顺序**：那条链才是串行顺序 | ✅ 表示先后 |

**实际事故**：T-10 的「依赖」列原写 `T-03`（已完成），据此被提前执行，
**跳过了关键路径上的 T-06 → T-07 → T-08（组 B，出 D4 的 LER 数字）**。
已把 T-10 的依赖列改为 `T-08（关键路径串行）`，并在此明确两条纪律的差别。
**开工前先看关键路径，再看依赖。**

---

## §6 任务状态定义

| 状态 | 含义 |
|---|---|
| ⬜ `todo` | 已登记，未开始 |
| 🟡 `doing` | 正在做（同一时间最多 1 条为「当前任务」） |
| 🔴 `blocked` | 被阻塞，必须在 §3 找到对应问题行 |
| ✅ `done` | 满足验收标准 |
| 🚫 `dropped` | 放弃，必须写明理由（见 §4 已砍任务） |

---

## §7 阶段二任务池（只登记，**不启动**）

> 启动条件见 `BASELINE.md` §4.1。**阶段一期间一律不碰。**

| 编号 | 任务 | 门槛 |
|---|---|---|
| T-2xx | 重做冠层表达（同时满足叶面积守恒 + 能量守恒 + 侧向受光）→ 解 P1/P4/P5 | P1 |
| T-2xx | 参数出处逐条核实并登记 | P3 |
| T-2xx | 与文献定量对照（报告全部结果） | P4 |
| T-2xx | 敏感性分析（消光系数 × 辐射利用效率） | P5 |
| T-2xx | 不确定性区间输出 | P6 |
| T-2xx | 日累计积分（`pvlib` 逐时）替换等效日照时长 6 h | — |
| T-2xx | 带型搜索 + 帕累托前沿 | — |
| T-2xx | 农机幅宽硬约束 | — |
| T-2xx | 前端工程（TypeScript + Vite + Three.js）+ 后端 + 在线部署 | — |
| T-2xx | Blender 渲染管线（正式版） | — |
| T-2xx | 用户调研（≥ 5 人深访） | 启动前 |

---

## §8 分工建议（3 人）

| 角色 | 主要负责 | 关键路径上的任务 |
|---|---|---|
| **A · 计算** | 辐射模型、光分布算法 | T-01 ~ T-05（已完成） |
| **B · 数值** | 产量换算、LER、批量对比 | T-06 ~ T-09（已完成） |
| **C · 呈现** | 出图、三维、交互、录屏 | **T-11、T-17、T-18、T-14**（剩余全部） |

> **注意**：T-04（判据 C1）由 A 实现，**必须由 B 或 C 独立复核**。
> 自己写的自检不能只由自己确认。

---

## 附录 · 用户验证反馈记录

> T-15 完成后填写。

| 日期 | 受访者（角色） | 看懂了吗 | 最想问的问题 | 建议 |
|---|---|---|---|---|
| | | | | |

---

## 附录 · 历史留档指针

T-04 的完整过程记录（含 3 条内核路线、5 个已修缺陷、判据自身的 2 处错误）
已按「知识归口」原则移入 `DESIGN.md` 附录 A / A.1 / A.2，本文件不再重复。
需要追溯某一轮的具体数值与决策 → 看提交历史 + `DESIGN.md` 附录 A.2。
