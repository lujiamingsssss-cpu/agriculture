# 任务（TASKS）

> **本文件的唯一职责**：记录**计划原点**、**路线**、**任务池**。
> **不属于本文件**：设计理由（→ `DESIGN.md`）、范围仲裁（→ `BASELINE.md`）、
> 演示交付物与提交卫生（→ `DEMO-HANDOVER.md`）。
>
> **本文件是当前任务的唯一真相源。任何未登记在此的工作，视为未获授权。**

---

## §1 计划原点（v0.3：**以当前状态为原点**）

| 项 | 值 |
|---|---|
| 计划版本 | **v0.3（重定基线）** —— 取代旧的 T-00 ~ T-18 编号体系 |
| 原点日期 | **2026-09-18** |
| 原点状态 | 光已算准（能量守恒 + 南北/东西对比成立）；**单时刻**的分带数值已出 |
| 原点缺口 | 🔴 **时间维度完全没有** |
| 阻塞 | **无** |
| 待拍板 | **无**（三个决定已定，见 §2.4） |
| 上一提交 | `e101e36` |

> **为什么以当前状态为原点**：演示方向发生了实质变化（输入轴从"带型参数"变成
> "时间 + 行向 + 地点 + 日期"），旧路线是在**旧方向上**推出来的。
> 若继续沿用旧编号往下排，会得到"看起来还是老路"的假象 ——
> 实际连**起点**都错了（新方向的地基是时间，而仓库里没有时间）。
> 所以：**旧编号降为追溯用，新计划从今天重新起算。**

### 1.1 既有资产（已结转，**不重做**）

> 这些是已完成工作的产物。在新计划里它们是**工具**，不是步骤。

| 资产 | 位置 | 在新计划里的角色 |
|---|---|---|
| 计算环境 + 辐射模型选型 | `pyproject.toml` / `uv.lock` / `TECH-STACK.md` | 直接用（`pyhelios3d` + OptiX） |
| 条带场景 → 几何注入 | `stripcore/scene.py`、`scenario.py`、`geometry.py` | **太阳角输入口要被 T-101 改造**，其余复用 |
| 逐层辐射 + 能量守恒守门人 | `stripcore/radiation.py` | T-102 复用它做批量；`assert_energy_conservation` 是每时刻的硬门禁 |
| **剖面图渲染函数** | `stripcore/profile.py`、`scripts/make_profile_figure.py` | T-103 出并排图、T-106 出"该时刻的下钻视图"**直接复用** |
| 生物量 / LER / 批量对比 | `biomass.py`、`ler.py`、`batch.py` | 判据 D4 用；**LER 的已知偏离要如实报告** |
| 8 个验收脚本 | `scripts/verify_t0*.py`、`make_profile_figure.py` | 改内核时**全部**要跑（`DEMO-HANDOVER.md` §10.1 门禁 3） |
| 已测定的数值与边界 | `DEMO-HANDOVER.md` §3–§6、`DESIGN.md` 附录 A | 直接引用。⚠️ **逐时数字待 T-103 变成可一键重跑** |

### 1.2 原点缺口（新计划真正要补的）

| 能力 | 现状（已实测） | 归属 |
|---|---|---|
| 地点 / 日期 / 时刻 → 太阳位置 | 🔴 **完全没有**：太阳角硬编码在 `scenarios/*.json`（60°/180°），`radiation.py` 明写"不含时间"，`stripcore` 里**零个** `pvlib` 调用 | **T-101** |
| 一天多时刻批量 | 🔴 只能单场景单时刻跑 | **T-102** |
| 两作物受光比 **vs 时刻** | 🔴 只有单时刻的分带数值 | **T-103** |
| 南北 / 东西**并排** | ⚠️ 两张图已生成，只差并排 | **T-103** |
| 三维演示 | 🔴 无 | **T-105** |
| 交互外壳 | 🔴 无 | **T-106** |
| 录屏 | 🔴 无 | **T-107** |

---

## §2 新路线（从原点出发）

### 2.1 关键路径

```
T-101 → T-102 → T-103 ⭐ → T-104（试看门禁）→ T-105 → T-106 → T-107
 地基     批量     曲线       看不懂就改呈现      三维    外壳    录屏自检
```

> **只按这条链执行。** 不要用"依赖"列判断先后（见 §5，已因此出过事故）。

### 2.2 任务表

| 编号 | 任务 | 产出 | 可证伪判据（先写判据，再实现） | 预估 | 依赖 |
|---|---|---|---|---|---|
| **T-101** 🔴 | **太阳位置接入计算层** | `pvlib` 驱动的太阳位置模块，替换场景里的硬编码角度 | ① 计算层用的太阳向量与 `pvlib` 独立算出的夹角 **< 0.01°**；② 跨 12 个中国农业站点不报错；③ 极昼/极夜有**明确定义的行为**（不是崩溃）；④ 南半球可用 | 0.5d | 原点 |
| **T-102** | **时域扫描** | 一天 N 个时刻的批量运行 API | ① 逐 10 分钟的 **73 个可算时刻 0 失败**；② **每个时刻**能量守恒 ≤ 1；③ 相邻时刻结果**连续**（无跳变） | 0.5d | T-101 |
| **T-103** ⭐ | **日变化曲线 + 南北/东西并排** | 两作物受光比随时刻的曲线（南北/东西双线）+ 并排剖面图 | ① 南北曲线"峰值/谷值" **> 10**（尖峰）、东西 **< 2.5**（平稳）；② 并排图**同参数、同色标、同尺度**；③ 图上标注**单位与口径** | 1d | T-102 |
| **T-104** 🚦 | **立即试看（门禁，非开发任务）** | 试看记录（写本文件附录） | 找 **1–2 位非团队成员**看曲线+并排图，对方能**主动**说出"南北中午有个尖" | 0.2d | T-103 |
| **T-105** | **三维演示**（demo 必须有） | Blender headless 渲染的 3D 场景图，**颜色来自计算值** | ① 玉米高、大豆矮**可辨**；② 色标标**物理量 + 单位**；③ 图注写「**亮度不代表 PAR**」；④ **不放照向冠层的真实 Sun 灯** | 1d | T-103 |
| **T-106** | **交互外壳** | 可现场点击的页面（地点 / 日期 / 时间滑块 / 行向开关 + 曲线 + 该时刻热力图 + 三维图） | ① 组件清单见 `DEMO-HANDOVER.md` §4；② 不支持的输入**显式标注**"本原型不支持"；③ 太阳高度角 ≤ 0° 时**禁用滑块**并提示；④ 拖动近实时 | 1d | T-105 |
| **T-107** | **录屏 + 收尾自检** | 3 分钟视频 + D1–D6 自检表 | 成片可播放，含南北/东西对比与三维镜头；D1–D6 全部勾选 | 0.5d | T-106 |

**T-104 是门禁**：它是"演示效果为先"的落点 —— 若试看失败，**先改 T-103 的呈现方式**，
再决定 T-105 / T-106 怎么做，不允许带着"可能看不懂"往下建。

### 2.3 与旧路线相比，实质变化（不是改标签）

| # | 变化 | 旧 | 新 |
|---|---|---|---|
| 1 | **起点** | 从"南北/东西并排图"（旧 T-11）开始 | 从**太阳位置接入**（T-101）开始 —— 因为时间维是唯一地基，而仓库里没有 |
| 2 | **地基是否独立验收** | 无此项 | T-101 独立立项 + 独立判据（它改的是**内核输入口径**，属单向门） |
| 3 | **T-10 热力图的地位** | 前置任务 | 降为"曲线上某点的**下钻视图**"，复用函数、不重做 |
| 4 | **试看时机** | 倒数第 2 步（录屏之后） | **提到第 4 步**（出曲线之后立刻做） |
| 5 | **三维的位置** | 排在录屏前 | 排在交互外壳**之前**（外壳要嵌入三维图） |
| 6 | **T-05 降级方案** | 一直挂着 | 🚫 **正式关闭**（A2 已判定成立） |

### 2.4 已定的三个决定（v0.3）

| # | 决定 | 内容 | 落到了哪里 |
|---|---|---|---|
| **1** | **阶段二仍做"推荐带型"（方案 A）** | 阶段一只验证"光分配**可算可看可比较**"这一半；"给出推荐带型"那一半归阶段二，依赖**重做冠层表达**。阶段一**不得宣称**已具备推荐能力 | `BASELINE.md` §1（北极星切成两半）、§4.1（追加认领条件）、§5 In Scope |
| **2** | **demo 必须有三维演示** | 三维是**必做**，并新增验收判据 **D6** | `BASELINE.md` §3.2 D6、§3.3 订正（"Blender 阶段二不做"已修）、`TECH-STACK.md` DR-09 |
| **3** | **以当前状态为新计划原点** | 旧编号降为追溯；新计划从 §1 的原点出发 | 本文件 §1 / §2 |

---

## §3 旧编号 → 新计划映射（追溯用，**不再是计划**）

| 旧编号 | 原内容 | 新归属 |
|---|---|---|
| T-00 ~ T-10 | 已完成的开发（环境、场景、辐射、判据 C1、生物量、LER、批量、剖面图） | ✅ **结转为既有资产**（§1.1），不重做 |
| **T-11** | 时间维度 + 南北/东西并排 | 拆为 **T-101 / T-102 / T-103**（原先被塞进一个任务号，是本次重排的直接原因） |
| **T-17** | 三维渲染 | **T-105** |
| **T-18** | 交互演示工具 | **T-106** |
| **T-14** | 3 分钟录屏 | **T-107** |
| **T-15** | 找人试看 | **T-104**（提前） |
| **T-16** | 收尾自检 | **T-107** |
| **T-05** | 降级方案（A2 不成立时用） | 🚫 **正式关闭**（A2 已成立） |
| T-12 / T-13 | Three.js 前端页面 | 🚫 已砍（`TECH-STACK.md` DR-08 / DR-09 为替代） |

---

## §4 问题 ↔ 方案绑定表 ⭐

> 每一次返修都对应下表某一行。**改代码前先在这一行找到原因**；
> 新增问题必须先登记，再动手（`AGENTS.md` §2「先写可证伪的判据」）。

| # | 问题（已实测） | 现象 / 数值 | 绑定方案 | 实施位置 | 状态 | 什么情况下会**返修** |
|---|---|---|---|---|---|---|
| **P1** | 行比 / 带宽 / 株高 / LAI **不响应** | 逐个改输入，最大变化 ≤ 1% | 不暴露为控件；界面显式标注"本原型不支持"；输入轴改为时间/行向/地点/日期 | T-103、T-106；`DEMO-HANDOVER.md` §4 第 8 项 | ✅ 已定 | **决定 1（方案 A）下**：阶段二重做冠层表达（`DESIGN.md` 附录 A 第 9/11/13/15/16 行）后，这些控件才可重新开放 —— 这是**阶段二"推荐带型"的前提** |
| **P2** | **连续行向角度能量守恒失效** | 15°–75° 区间吸收/入射 = **1.19 ~ 2.80**（> 1，物理不可能）；0°/90° 为 0.9844 | 只开放 **0° / 90°** 两档开关 | T-106；`PARAMETERS.md` §1 | ✅ 已定 | 找到自洽的侧向遮挡配置（附录 A 第 13 行）后可放开 |
| **P3** | C1 **字面成立但幅度仅 +0.99%** | 冠层总量 16374.0 W vs 16212.9 W；玉米带 **−52.8%**、大豆带 **+121.7%** | 演示口径用「**带间再分配**」；主指标用大豆/玉米受光比（1.05 vs 0.22） | T-103 的图与文案；`BASELINE.md` §1/§5 | ⚠️ 口径已改（v0.3 连 `BASELINE.md` §1 的论证方式一起改了） | 若评审要求以"**总量**更多"论证 → 只能回答"被几何上界锁死，这是已知边界" |
| **P4** | **LER ≈ 1.0**，低于文献 1.0–1.4 | 标准口径 **0.9995** | **如实登记，禁止调参**；判据 D4 要求**数字与偏离一起报** | `DESIGN.md` 附录 A 第 15 行；判据 D4 | ⚠️ 已知偏离 | 若要把 LER 当卖点 → 必须先解决 P5 |
| **P5** | **叶片"遮挡"与"不重复计数"互斥** | 单面 → 不阻断射线；双面 → 重复计数（东西向 137.5%）。三条内核路线均已实测失败 | 阶段一**不碰内核**；三维只做**形态示意 + 计算值着色**，不宣称侧向受光 | `DESIGN.md` 附录 A.2；`DEMO-HANDOVER.md` §8 | 🔴 阶段一不解决 | 被追问"为什么东西行向大豆受光少"的机制 → 只能用 P3 的再分配口径答，**不得编造侧向受光** |
| **P6** | 太阳高度角 **≤ 0° 触发断言** | `radiation.py` 的 `sz > 0` 检查；实测 −5°/0° 报错，0.2° 起可算 | UI 禁用滑块并显示"太阳未升起"；极昼/极夜同样处理 | T-101、T-106；`DEMO-HANDOVER.md` §5 | ⬜ **未实施**（T-101/T-106 落地） | 演示地点/日期落到夜间 → 演示前必须用目标地点日期预跑一遍 |
| **P7** | **两个 Python 解释器** | 项目 venv 3.11.9 vs Blender 内置 3.13.13 | 只能**文件交换**（JSON） | T-105；`DEMO-HANDOVER.md` §7 | ✅ 已定 | 试图在 Blender 里 import `stripcore` → 必失败 |
| **P8** | **没有 MCP 客户端** | MCP add-on 在 TCP 9876 未监听 | 走 **headless**（`--background --factory-startup --python`） | T-105；`DEMO-HANDOVER.md` §9 | ✅ 已定 | 要求"实时联动三维" → 属阶段二 |
| **P9** | **提交信息 BOM 污染**（已 2 次） | `%s` 看不见 BOM | `-m` 不落盘 + 判首字符码点（65279 = 污染） | `DEMO-HANDOVER.md` §10.3；DR-06 | ✅ 已加防护 | 再次发生 → 检查是否用了 `Out-File -Encoding utf8`（PowerShell 5.1 会写 BOM） |
| **P10** | **非农业背景评委看不懂** | 用户原话：可视化差就看不出项目解决了什么问题 | 曲线 + 并排图 + 可拖滑块；色标标物理量+单位 | T-103、T-105、T-106 | ⬜ 待实施 | **T-104 试看失败即为触发** → 优先改 T-103 呈现，**不要**先加堆三维或动效 |
| **P11** | **三维与"阶段二不做"的边界冲突**（自造） | `BASELINE.md` §3.3 原写"Blender 阶段二不做"，却又规划了三维 | 订正 §3.3（阶段一做**静态出图**，正式管线属阶段二）+ 新增判据 D6 | `BASELINE.md` §3.2/§3.3；DR-09 | ✅ **v0.3 已修** | 若三维要升级为可交互/动画 → 属阶段二 |

---

## §5 执行顺序纪律（起因：T-10 曾被提前执行）

**任务必须按 §2.1 的「关键路径」顺序执行，不得仅凭「依赖」列判断可否开工。**

| 列 | 含义 | 是否表示执行顺序 |
|---|---|---|
| 依赖 | **技术前置**：缺了它任务做不出来 | ❌ 只表示可行性 |
| **关键路径** | **执行顺序**：那条链才是串行顺序 | ✅ 表示先后 |

**实际事故**：旧 T-10 的"依赖"列原写 `T-03`（已完成），据此被提前执行，
**跳过了关键路径上的 T-06 → T-07 → T-08**。教训：**开工前先看关键路径，再看依赖。**

---

## §6 阶段二任务池（只登记，**不启动**）

> 启动条件见 `BASELINE.md` §4.1。**阶段一期间一律不碰。**

| # | 任务 | 门槛 |
|---|---|---|
| 1 | ⭐ **重做冠层表达**（同时满足叶面积守恒 + 能量守恒 + **侧向受光**）→ 解 P1 / P4 / P5，**这是"推荐带型"的前提** | P1 |
| 2 | 参数出处逐条核实并登记 | P3 |
| 3 | 与文献定量对照（报告全部结果） | P4 |
| 4 | 敏感性分析（消光系数 × 辐射利用效率） | P5 |
| 5 | 不确定性区间输出 | P6 |
| 6 | 日累计 / 全生育期积分（`pvlib` 逐时）替换等效日照时长 6 h | — |
| 7 | **带型搜索 + 帕累托前沿**（"推荐带型"的实现本体） | 依赖第 1 项 |
| 8 | 农机幅宽硬约束 | — |
| 9 | 前端工程（TypeScript + Vite + Three.js）+ 后端 + 在线部署 | — |
| 10 | Blender 渲染管线（批量、动画、正式材质体系） | — |
| 11 | 真实用户调研（≥ 5 人深访，种粮大户 / 农技员） | 启动前 |

---

## §7 任务状态定义

| 状态 | 含义 |
|---|---|
| ⬜ `todo` | 已登记，未开始 |
| 🟡 `doing` | 正在做（同一时间最多 1 条为"当前任务"） |
| 🚦 `gate` | 门禁：通过才允许继续，失败则回到上游改呈现 |
| 🔴 `blocked` | 被阻塞，必须在 §4 找到对应问题行 |
| ✅ `done` | 满足验收标准 |
| 🚫 `dropped` | 放弃，必须写明理由（见 §3 映射表） |

---

## §8 分工建议（3 人）

| 角色 | 主要负责 | 新路线上的任务 |
|---|---|---|
| **A · 计算** | 太阳位置、时域扫描 | **T-101、T-102** |
| **B · 数值/呈现** | 曲线、并排图、三维 | **T-103、T-105** |
| **C · 交互/交付** | 交互外壳、录屏、试看组织 | **T-104、T-106、T-107** |

> **注意**：T-101 由 A 实现，**必须由 B 或 C 独立复核**（判据 ① 的夹角对照要独立算）。
> 自己写的自检不能只由自己确认。

---

## 附录 A · 试看记录（T-104 完成后填写）

> 目的：验"**非农业背景的人能否一眼看懂**"。这不是用户调研（那属阶段二）。

| 日期 | 受访者（角色） | 看懂了吗 | 能否主动说出"南北中午有个尖" | 最想问的问题 | 建议 |
|---|---|---|---|---|---|
| | | | | | |

---

## 附录 B · 历史留档指针

旧 T-04 的完整过程记录（含 3 条内核路线、5 个已修缺陷、判据自身的 2 处错误）
已按"知识归口"移入 `DESIGN.md` 附录 A / A.1 / A.2。
旧编号的详细任务卡 → 查 `git log`（`50f47ec` 及其之前的 `docs/TASKS.md`）。
